const initialState = {
    users:[
        {name:"Martin", email:"martinrobertson860@gmail.com"}        
    ]
}

const UserReducer =( state = initialState, action) => {
    return state
}

export default UserReducer;